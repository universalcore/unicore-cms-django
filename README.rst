Unicore CMS Django
==================

This is one to throw away.

Install on OSX
--------------

Install the packages in a virtualenv with pip.

    $ virtualenv ve
    $ source ve/bin/activate
    (ve)$ pip install -e .
